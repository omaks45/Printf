#include "main.h"
#include <string.h>
/**
*print_s - print a string to the stdout
*@s: string to be printed
*
*Return: number of chars written
*/
int print_s(char *s)
{
   int total = 0;

   for (int i =0; s[i] != '\0'; i++)
        total+= _putchar(s[i]);
   return (total);
}
